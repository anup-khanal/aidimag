{
  "name": "aidimag-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Documentation for aiDimag",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test": "echo \"No tests for docs-only package\" && exit 0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vite": "^8.1.5",
    "vitepress": "^1.6.4"
  },
  "overrides": {
    "vitepress": {
      "esbuild": ">=0.24.3",
      "vite": ">=6.4.3"
    }
  }
}
